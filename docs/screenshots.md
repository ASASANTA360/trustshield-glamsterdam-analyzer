# TrustShield AI Screenshots

Use this page as the source of truth for grant-submission screenshots. Replace each placeholder with an exported image after running the Next.js dashboard locally.

## Capture checklist

1. Run `npm install` if dependencies are not present.
2. Start the app with `npm run dev`.
3. Open `http://localhost:3000`.
4. Use the USDC and WETH example buttons to capture realistic Ethereum demo states.

## Homepage screenshot

**Placeholder:** `docs/assets/homepage.png`

Capture the hero section showing the value proposition, primary call to action, and platform metrics.

## Scan form screenshot

**Placeholder:** `docs/assets/scan-form.png`

Capture the contract scanner with the USDC and WETH example buttons visible.

## Result dashboard screenshot

**Placeholder:** `docs/assets/result-dashboard.png`

Capture the Trust Score, Security Grade, Risk Level, Contract Overview, Findings table, and Recommendations section after scanning USDC on Ethereum.

## Analytics screenshot

**Placeholder:** `docs/assets/analytics.png`

Capture aggregate scan analytics once scan history is available through the analytics endpoint or dashboard view.

## Tested contracts

- USDC on Ethereum: `0xA0b86991c6218b36c1d19D4a2e9Eb0cE3606eB48`
- WETH on Ethereum: `0xC02aaA39b223FE8D0A0e5C4F27eAD9083C756Cc2`
